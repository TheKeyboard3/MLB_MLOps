# H2O AutoML Diagnosis Classification

Проект для класифікації медичних діагнозів (бінарна класифікація) з використанням H2O AutoML та трекінгом експериментів у MLflow.

## 📋 1. Попередні вимоги (Prerequisites)
- **OS:** Linux
- **Python:** 3.12+
- **Java:** JDK (необхідна для роботи H2O)
  - Встановлення на Ubuntu/Debian: `sudo apt install default-jdk`

## ⚙️ 2. Інсталяція

1. **Створіть віртуальне середовище:**
   ```bash
   python3.12 -m venv venv
   source venv/bin/activate
   ```
2. **Встановлення залежностей:**
   ```bash
   pip install -r requirements.txt
   ```
   
## 📂 3. Підготовка даних

1.  Розмістіть ваш вихідний CSV-файл у папку:
    `data/raw/`

2.  Перевірте `configs/config.yaml`:

      - Переконайтеся, що параметр `raw_path` вказує на правильне ім'я файлу (наприклад, `../data/raw/my_dataset.csv`).
      - Вкажіть правильну назву цільової колонки у `target_col`.

## 🚀 4. Запуск експериментів

### Етап 1: Baseline (Логістична регресія)

Запускає просту модель для встановлення базового рівня метрик.

```bash
# Тренування та збереження моделі
python src/train.py

# Оцінка та логування в MLflow
python src/evaluate.py
```

### Етап 2: H2O AutoML

Запускає автоматичний пошук найкращої моделі (обмеження часу: 15 хв).

```bash
python src/automl_run.py
```

## 📊 5. Перегляд результатів (MLflow UI)

Щоб порівняти моделі та переглянути графіки:

1.  Запустіть сервер MLflow:
    ```bash
    mlflow ui --backend-store-uri mlruns
    ```
2.  Відкрийте у браузері: [http://127.0.0.1:5000](https://www.google.com/search?q=http://127.0.0.1:5000)
